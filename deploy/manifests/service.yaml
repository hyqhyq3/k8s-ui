apiVersion: v1
kind: Service
metadata:
  name: k8s-ui
  namespace: k8s-ui
  labels:
    app: k8s-ui
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: k8s-ui
